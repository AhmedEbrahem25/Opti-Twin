.env
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
node_modules/
.next/
out/
.DS_Store
*.log
logs/
.idea/
.vscode/
*.swp
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Trained ML model checkpoints — large binary, regenerated by train_ppo.py.
# Keep the directory itself (via .gitkeep) but exclude actual checkpoints
# except a single demo-pinned one.
ai_engine/models/*.zip
!ai_engine/models/opti_twin_ppo.zip

data/meili/